# Build / deps (the recovered app)
node_modules
dist
dist-ssr
*.local
.vite
*.tsbuildinfo

# OS junk
.DS_Store

# Obsidian per-machine workspace state (ignore local layout; keep plugin config)
obsidian-vault/.obsidian/workspace.json
obsidian-vault/.obsidian/workspace-mobile.json
obsidian-vault/.obsidian/cache
.trash/
